<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="24dp"
    android:background="@color/ux_color_primary"
    tools:context=".haushalt_activity.AddHaushaltActivity">

    <!-- Zurück-Button (größere Touch-Fläche, Kontrast + A11y) -->
    <ImageView
        android:id="@+id/back_button"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:padding="12dp"
        android:contentDescription="Zurück"
        android:clickable="true"
        android:focusable="true"
        app:tint="@color/ux_color_on_primary"
        app:srcCompat="@drawable/outline_arrow_back_24"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- Titel / Instruktion -->
    <TextView
        android:id="@+id/textView6"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="Bitte geben Sie Ihren Haushaltsnamen ein:"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="@color/ux_color_on_surface"
        android:lineSpacingExtra="2dp"
        app:layout_constraintTop_toBottomOf="@id/back_button"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Eingabefeld (breit, klare Lesbarkeit, Hint statt Platzhalter-Text) -->
    <EditText
        android:id="@+id/hName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:ems="10"
        android:hint="Haushaltsname"
        android:inputType="textCapWords|textPersonName"
        android:importantForAutofill="yes"
        android:autofillHints="name"
        android:textColor="@color/ux_color_on_surface"
        android:textColorHint="@color/ux_color_outline"
        android:backgroundTint="@color/ux_color_outline"
        android:padding="12dp"
        app:layout_constraintTop_toBottomOf="@id/textView6"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Primär-Button (Material, Akzentfarbe, große Fläche) -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/hAddName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:layout_marginBottom="24dp"
        android:text="Haushalt erstellen"
        android:textAllCaps="false"
        android:minHeight="48dp"
        app:cornerRadius="12dp"
        app:iconGravity="textStart"
        app:rippleColor="@color/ux_color_on_primary"
        app:layout_constraintTop_toBottomOf="@id/hName"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:backgroundTint="@color/ux_color_accent"
        android:textColor="@color/ux_color_on_primary" />

</androidx.constraintlayout.widget.ConstraintLayout>
